# Phase 2A — Auth and User Session

- Files changed: `lib/models/user_model.dart`, new `auth_link_model.dart`, `user_directory_model.dart`, and `auth_session.dart`; `lib/services/auth_services.dart`, `lib/services/user_services.dart`, `lib/controllers/auth_controller.dart`, `lib/controllers/member_controller.dart`, `lib/views/splash_screen.dart`, `lib/views/login_screen.dart`, `lib/views/member_list_screen.dart`, `lib/views/profile_screen.dart`; four model/session test files. Removed the obsolete scaffold `test/widget_test.dart`.
- Legacy fallback removed: normal admission no longer reads `users/{authUid}`, queries `users.auth_uid`, accepts legacy `role`, position, committee year, directory data, or grants the old admin exception. Password is sent to Firebase unchanged.
- Session states: `unauthenticated`, `emailUnverified`, `unlinked`, `linkInactive`, `userMissing`, `userInactive`, `loginDisabled`, `invalidRole`, `admitted`, `error`. Permission/network/schema failures map to `error`, never `userMissing`.
- Tests: `flutter test` — **16 passed, 0 failed**, synthetic data only. Covers strict User/AuthLink/UserDirectory schemas, missing and malformed security fields, Timestamp-only parsing with no current-time fallback, invalid roles, legacy-field rejection, and session-state mapping.
- Analyze: `flutter analyze --no-pub` — **exit 1**, 9 existing information-level lints; **0 errors, 0 warnings**.
- Compatibility gap: the current legacy production admin intentionally fails strict admission until a separate reviewed bootstrap/migration creates an exact v1.2.1 User and active auth link. Remaining legacy member/committee display and business-service paths are outside Phase 2A and receive no authorization bypass.
- **Production deploy NOT performed.** No production Firebase/Auth/data, migration, Storage, Functions, Blaze, or UI redesign was performed.
- Exact next task: Phase 2B — replace remaining Flutter legacy role/committee User usage with v1.2.1 access-role capability checks and `user_directory`/committee-assignment reads, without adding migration bypasses.
